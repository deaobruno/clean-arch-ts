import BaseController from '../../../src/adapters/controllers/BaseController'

export default {
  start(): void {
    
  },
  stop(callback?: (error?: Error) => void): void {
    
  },
  get(path: string, controller: BaseController) {
    
  },
  post(path: string, controller: BaseController) {
    
  },
  put(path: string, controller: BaseController) {
    
  },
  delete(path: string, controller: BaseController) {
    
  },
}
